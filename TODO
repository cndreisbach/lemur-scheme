- create separate root environment and scope (frame?) classes
- scope inherits from Hash
- forms (syntax) should go in same environment as everything else
- remove built-ins from main lemur.rb and put in root env
- let